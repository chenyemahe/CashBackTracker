<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="add_order_text_style" parent="TextAppearance.AppCompat.Medium">
        <item name="android:background">@color/add_key_background</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:layout_height">@dimen/tb_row_hight</item>
    </style>

    <style name="add_order_text_style_2" parent="TextAppearance.AppCompat.Medium">
        <item name="android:background">@color/add_key_background</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:layout_height">@dimen/tb_row_hight</item>
    </style>

    <style name="add_order_text_style_3" parent="TextAppearance.AppCompat.Medium">
        <item name="android:layout_height">@dimen/tb_row_hight</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="add_order_edit_text_style">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingLeft">@dimen/list_margin</item>
        <item name="android:layout_height">@dimen/tb_row_hight</item>
    </style>

    <style name="add_button">
        <item name="android:background">@color/button_color</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="main_order_text_style">
        <item name="android:background">@color/add_key_background</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:layout_height">match_parent</item>
    </style>
</resources>
